<p>
  <strong>Схема:</strong>
  <%= @layout.name %>
</p>

<div id="layout_elements">
<%= render "layout_elements" %>
</div>

<%= hidden_field :lay_el, :layout_id, value: @layout.id  %>
<%= select 'lay_el','element_id', content_tag(:option,'Элемент...',:value=>"")+options_for_select(@elements.collect{ |e| [e.name, e.id] }) %>
<%= number_field :lay_el, :count, value: 1, class: "short_num" %>
<span class="btn-sm btn-success">Добавить</span>
