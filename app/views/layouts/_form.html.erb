<%= simple_form_for(@layout) do |f| %>
  <div class="form-inputs">
    <%= f.input :name, label: 'Имя',:input_html => {:class => 'w80' } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Записать', class: 'btn-success' %>
  </div>
<% end %>
